name: "workflow_commands"

on: [push]
jobs:
    my_job:
        runs-on: ubuntu-latest
        steps:
          - name: "group logging"
            run: | 
                echo "::group:: My group"
                echo "message 1"
                echo "message 2"
                echo "::endgroup::" 
